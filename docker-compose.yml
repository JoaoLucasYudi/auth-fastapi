version: "3.1"

services:
  postgresql:
    image: postgres
    environment:
      - "POSTGRES_USER=admin"
      - "POSTGRES_PASSWORD=123"
      - "POSTGRES_DB=dbpostgres"
    ports:
      - 5432:5432
    volumes:
      - pg-data:/var/lib/postgresql/data
volumes:
  pg-data:
